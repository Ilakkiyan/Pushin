# Dependencies
node_modules/

# Frontend build output
dist/
dist-ssr/
*.local

# Rust / Tauri build output
src-tauri/target/
# Isolated target dir for running the LLM eval harness while the app holds target/ (locked .exe).
src-tauri/target-eval/
src-tauri/gen/

# Downloaded models & inference engine (fetched at runtime, large)
src-tauri/models/
*.gguf

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.*
!.env.example

# Signing keys & secrets — never commit. The Tauri updater private key lives in ~/.tauri (outside the
# repo); these patterns are a safety net in case a key is ever copied in. The *.pub is safe to share.
*.key
!*.pub
.tauri/
*.pem

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
.vscode/*
!.vscode/extensions.json

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Fine-tune experiment: generated weights/data (regenerable, large)
/finetune/out/
/finetune/data/
/unsloth_compiled_cache/

# Android build helper scripts (machine-specific absolute paths; see memory mobile-cross-compile)
/and-*.bat
